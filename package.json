{
    "name": "gymrat-platform",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build:frontend": "cd frontend && CI=false npm run build",
        "build:backend": "echo 'No build step required for backend'",
        "build": "npm run build:frontend",
        "start": "cd backend && npm start"
    }
}